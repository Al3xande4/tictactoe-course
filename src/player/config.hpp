#pragma once

namespace ttt::my_player
{
    namespace config
    {
        enum BoardOps
        {
            HEIGHT = 20,
            WIDTH = 20,
            TOTAL_CELLS = HEIGHT * WIDTH,
        };
    }
}